source "https://rubygems.org"

# This will help ensure the proper Jekyll version is running.
gem "jekyll"
gem "json"

group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-seo-tag"
  gem 'generate_quote_json', path: './_plugins/generate_quote_json'
end
